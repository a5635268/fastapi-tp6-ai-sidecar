# FastAPI Enterprise 应用依赖

# 核心框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# 数据校验
pydantic>=2.5.0
pydantic-settings>=2.1.0

# 数据库
tortoise-orm>=0.20.0
aiomysql>=0.2.0    # MySQL 异步驱动
asyncpg>=0.29.0   # PostgreSQL 异步驱动

# 缓存
redis>=4.5.0      # Redis 异步客户端（注解系统依赖）

# Excel 处理
openpyxl>=3.1.0   # Excel 导入导出工具

# 安全认证
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# HTTP 客户端
httpx>=0.26.0

# 开发工具
python-multipart>=0.0.6  # 表单支持

# LangChain AI 框架
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# 网页抓取与解析
beautifulsoup4>=4.12.0
html2text>=2024.2.26
lxml>=4.9.0

# 图片处理
oss2>=2.18.0         # 阿里云 OSS SDK
pillow>=10.0.0       # 图片处理 (长图切片)

# 测试框架
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.26.0        # 已存在于 HTTP 客户端，复用于异步测试
fakeredis>=2.20.0    # Redis Mock
